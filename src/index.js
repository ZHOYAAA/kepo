import React from "react";
import ReactDOM from "react-dom";
import "./theme.css"; // Import tema global
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
